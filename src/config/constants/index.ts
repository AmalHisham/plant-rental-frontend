export const MAX_PLANT_IMAGES = 10;

// Temporary rollout flag for unfinished AI UX (preview/chat).
// Keep false in production until the end-to-end flow is fully ready.
export const ENABLE_AI_PREVIEW = true;
export const COUNTRY_CODE_IN = '+91';
